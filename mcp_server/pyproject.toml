[project]
name = "moscowle-mcp-server"
version = "1.0.0"
description = "Servidor MCP para Moscowle IA - Acceso a datos de terapia digital"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.0.0",
    "httpx>=0.27.0",
]

[build-system]
requires = ["setuptools>=65.0"]
build-backend = "setuptools.build_meta"
